.DS_Store
Thumbs.db

# Root-level dev dependencies only — bundled libraries (e.g. the
# plugin-update-checker) ship their own vendor/ that must stay tracked.
/node_modules/
/vendor/

.idea/
.vscode/

# Claude Code: rechnerlokale Permission-Caches / Session-Settings.
# Dauerhafte Projektregeln gehoeren NICHT hierhin, sondern in CLAUDE.md
# (siehe MULTI_PC_SETUP.md).
.claude/

*.zip

# Build-Ausgabe (tools/build-zip.ps1) — bleibt bewusst lokal.
# Kein Test-/Release-ZIP wird je versioniert.
/_BUILD_OUTPUT/

tmp/
logs/
